#include<stdio.h>

int main()
{
int a , b ;
float c;
printf(" Enter two numbers : ");
scanf("%d%d",&a,&b);
if (b=0)
{ printf("Can't be divided / invalid division");
}
else
  c = a/b ;
  printf("Division = %f",c);

  return 0;

  }

